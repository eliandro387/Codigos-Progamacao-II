void imprimeLista() {
	Nos *atual;
	atual = inicio;
	while(atual){
		printf("%d",atual->valor);
		printf(" ponteiro anterior %p",(void*)atual->anterior);
		printf(" proximo ponteiro %p\n",(void*)atual->proximo);
		atual=atual->proximo;
	}
}
